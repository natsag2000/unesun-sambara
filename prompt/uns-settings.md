First analyze this project. It is a wasm application, which can write horizontal and vertical. Create a backlog prompt for refactor to make this editor supports more features/extensions. Adhere to best practices for text editor. Divide implementation in subsections. Every subsections has a ID. ID is useful if it broken or lost of context you can continue without problem. Create a description at the beginning how to work with this prompt, so that you do not have to search. Every section must have a process mark. 

Settings Requirement

- Settings button (icon) should be in top bar. if it clicked modal settings window should show multiple settings options.
- Dropdown list for Font face. There should be preview Text which shows in selected Font.
- Font size change options. The Preview Text can also show affected font size
- Color picker for Font.
- Color picker for Background.
- Checkbox for activate/deactivate to show line/column number

Technical Requirement
- use Tailwind CSS for styling
- organize the code so that more feature/extensions in the future easily supported.


